# Environment variables for the Vici-Monitor-Medalert backend server

# A strong, unique secret for signing JSON Web Tokens (JWTs)
# This should be a long, random string to ensure security.
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secure-random-secret-key-here
NODE_ENV=development
